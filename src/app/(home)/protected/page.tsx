const Page = () => {
    return (
        <div>
        <h1>Protected Page</h1>
        <p>This page is protected and only accessible to authenticated users.</p>
        </div>
    );
}

export default Page;